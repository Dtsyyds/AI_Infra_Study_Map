暴力解法需要检查多少组元素，时间复杂度是什么？
遍历到当前数字 x 时，真正需要查找的目标是什么？
为什么哈希表应先查询再写入，而不是无条件先写入？
输入 [3, 3]、目标值 6 时，怎样保证不会使用同一个元素两次？

1. 两数之和暴力求解检查 n + n-1 + n-2 + ... + 1 = n(n+1)/2 组元素，时间复杂度为 O(n^2)。
2. 遍历到当前数字 x 时，真正需要查找的目标是 target - x。
3. 先查询再写入是避免重复查询到自身
4. 先查询再写入,这样在查找的时候就不会查到自身，保证了不会使用同一个元素两次。