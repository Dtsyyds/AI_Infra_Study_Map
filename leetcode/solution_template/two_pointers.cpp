/*
双指针本质上是利用两个指针对数据结构（通常是数组或链表）进行协同遍历，将暴力解法中的两层循环优化为一层

1. 相向指针：适用于有序数组，寻找满足特定条件的元素对
left指向头，right指向尾，根据判断条件决定左右指针向中间移动
前提必须是单调性数组，乱序数组排序后再使用

2. 快慢指针：适用于链表中的环检测，找中点，删除倒数第N个元素等回字，数组中的原地去重
快指针每次移动两步，慢指针每次走一步
环检测：若链表有环，快慢指针必定在环内相遇
找中点：快指针走到终点时，慢指针刚好走到中点
数组去重：快指针负责探路找新元素，慢指针负责维护有效区域的边界

3. 滑动窗口：同向快慢指针，适用于子数组/子字符串问题，求最长或最短连续满足条件
right指针不断向右扩大窗口，当窗口满足条件时，left指针向右移动缩小窗口，寻找最优解

*/

class Template
{
    public:
    void func()
    {
        // 双指针模板
    }
};